<label class="relative inline-block">
	<input type="checkbox" role="switch" class="opacity-0 size-0" />
	<span class="slider"></span>
</label>

<style>
	/* Hide the default checkbox */
	/* .toggle input {
		opacity: 0;
		width: 0;
		height: 0;
	} */

	/* The container for the toggle */
	.toggle {
		position: relative;
		display: inline-block;
		width: 60px;
		height: 34px;
	}

	/* The slider */
	.slider {
		position: absolute;
		cursor: pointer;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-color: #ccc;
		transition: 0.4s;
		border-radius: 34px;
	}

	/* The slider handle */
	.slider:before {
		position: absolute;
		content: "";
		height: 26px;
		width: 26px;
		left: 4px;
		bottom: 4px;
		background-color: white;
		transition: 0.4s;
		border-radius: 50%;
	}

	/* Styles for when the checkbox is checked */
	input:checked + .slider {
		background-color: #2196f3;
	}

	input:checked + .slider:before {
		transform: translateX(26px);
	}
</style>
